#pragma once
#include <stdio.h>
#include <iostream>
#include <string>
#include <vector>
using namespace std;
class FileHelper
{
public:
	FileHelper();
	~FileHelper();
	/*static vector<string> splitString(string &str, string splitRegex);
	static vector<pair<string, string>> getConfigBlockParams(string &configFile, string blockName);
private:
	static bool isConfigBlock(string &line);
	static string getConfigBlock(string &configFile, string blockName);
	static vector<pair<string, string>> getConfigParams(string &configFile);*/
};

